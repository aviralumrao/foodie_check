# Legal Metrology Packaged-Commodity API

A simple Go REST API for compliance checking.

## Setup
1. Setup PostgreSQL.
2. Create database `sih_db`.
3. Set `DATABASE_URL` environment variable.
4. Run migrations in `migrations/001_init.sql`.
5. Run the API: `go run .`
